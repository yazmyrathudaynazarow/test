swaggerURL
    http://localhost:2121/swagger/index.html#/

-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------
1. make docker-run
2. sudo docker exec -it testProject cqlsh

3. CREATE KEYSPACE IF NOT EXISTS test
WITH REPLICATION = { 'class' : 'SimpleStrategy', 'replication_factor' : 3 };

4. USE test;

5. CREATE TABLE IF NOT EXISTS videos (
    id INT,
    title TEXT,
    duration INT
);

6. make run